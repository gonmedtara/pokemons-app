import { ShadowCardDirective } from './shadow-card.directive';

describe('ShadowCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowCardDirective();
    expect(directive).toBeTruthy();
  });
});
