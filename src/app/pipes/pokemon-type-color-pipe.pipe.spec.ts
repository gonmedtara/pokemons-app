import { PokemonTypeColorPipePipe } from './pokemon-type-color-pipe.pipe';

describe('PokemonTypeColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PokemonTypeColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
